import BaseLayout from "../components/BaseLayout";

const Home = ({auth}) => {
    return (
        <BaseLayout auth={auth} title="Sign In">
            Home
        </BaseLayout>
    )
};

export default Home